type Query {
  item(id: ID!): Item
  items: [Item]
}

type Mutation {
  createItem(input: CreateItemInput!): Item
}

type Item {
  id: ID!
  content: String
}

input CreateItemInput {
  content: String!
}